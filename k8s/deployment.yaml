apiVersion: apps/v1
kind: Deployment
metadata:
  name: heart-api
spec:
  replicas: 1
  template:
    spec:
      containers:
      - name: heart-api
        image: heart-api
        ports:
        - containerPort: 8000
